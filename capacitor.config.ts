import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'music_player_1',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
